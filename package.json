{
  "name": "zytyan-blog",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "vitepress dev docs",
    "build": "npm run generate-index && vitepress build docs",
    "preview": "vitepress preview docs",
    "generate-index": "node scripts/generate-index.mjs"
  },
  "dependencies": {
    "markdown-it-mathjax3": "^4.3.2",
    "vitepress": "^1.6.4"
  }
}
